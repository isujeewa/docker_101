ADD instruction in a Dockerfile is used to copy files from your local machine to the Docker image. It also allows you to fetch files from URLs and automatically extract compressed files.

docker build -t add-html-demo .
docker run add-html-demo

