The EXPOSE instruction in Docker is used to tell Docker that the container will listen on a specific port. This is primarily for documentation purposes, but it also helps when running containers with port mappings.
command

EXPOSE 8080